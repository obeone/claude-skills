apiVersion: v2
name: CHANGEME
description: A Helm chart for CHANGEME
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: common
    repository: https://bjw-s-labs.github.io/helm-charts
    # v5.0.1 (latest). For Kubernetes < 1.31 or Helm < 3.18, pin to 4.6.0.
    version: 5.0.1
