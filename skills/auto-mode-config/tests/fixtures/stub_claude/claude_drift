#!/bin/sh
# Stub `claude` binary: critique emits an unknown ## section header.
case "$1" in
  --version)
    echo "claude 2.1.133"
    exit 0
    ;;
  auto-mode)
    case "$2" in
      critique)
        if [ "$3" = "--help" ]; then
          cat <<'EOF'
Usage: claude auto-mode critique [options]

Options:
  --settings <path>
EOF
          exit 0
        fi
        cat <<'EOF'
# Critique of Custom Rules

## Severe issues

### 1. Made-up section

Upstream contract drift.

## Smaller issues

### 1. Minor

Body.
EOF
        exit 0
        ;;
    esac
    ;;
esac
echo "stub claude_drift: unhandled args: $*" >&2
exit 99
