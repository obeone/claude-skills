{
  "autoMode": {
    "environment": [
      "$defaults",
      "I work primarily in Python on macOS."
    ],
    "allow": [
      "$defaults",
      "Bash(git status:*)"
    ],
    "soft_deny": [
      "$defaults"
    ]
  }
}
