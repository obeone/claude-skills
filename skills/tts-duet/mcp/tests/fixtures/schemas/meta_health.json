{
  "description": "Probe MCP readiness. Returns package / protocol / SDK versions and a cached per-model availability map.",
  "inputSchema": {
    "additionalProperties": false,
    "properties": {},
    "type": "object"
  },
  "name": "meta.health",
  "outputSchema": {
    "additionalProperties": true,
    "properties": {
      "has_api_key": {
        "type": "boolean"
      },
      "mcp_version": {
        "type": "string"
      },
      "model_availability": {
        "type": [
          "object",
          "null"
        ]
      },
      "package_version": {
        "type": "string"
      },
      "protocol_version": {
        "type": "string"
      },
      "sdk_version": {
        "type": "string"
      },
      "status": {
        "enum": [
          "ok",
          "error"
        ],
        "type": "string"
      }
    },
    "required": [
      "status",
      "mcp_version",
      "package_version",
      "protocol_version",
      "sdk_version",
      "model_availability"
    ],
    "type": "object"
  }
}
